# semo 进行中任务（整合对话维护）

> 记录当前正在推进的任务、阻塞项、下一步动作。整合对话每次会话结束更新此文件。

## 当前阶段：MVP 第一波开发中
- 契约已评审通过并合并入 main（commit 7510191）
- 前端脚手架完成（feature/fe-scaffold, 9e32d37），待补同步契约
- 服务端待派发第一波任务
- 技术决策已定：DB=PostgreSQL，AI视觉=GLM-4V，文本(计划生成)=MiniMax M3(后续)

## 已完成里程碑
- 961c7e1 整合对话初始化骨架（shared/ai/mp/server+docs）
- 614954d API 对话补全 MVP 契约（user/food/record/upload）
- 7510191 整合对话评审通过，契约合并入 main
- 9e32d37 前端对话补全脚手架+Mock+4页面骨架

## 进行中任务
| ID | 任务 | 归属 | 状态 |
|---|---|---|---|
| F2 | 前端补同步契约（3对齐问题+upload接口） | 前端对话 | 进行中 |
| S1-S5 | 服务端第一波（user/food/record/upload+AI运行时） | 服务端对话 | 待派发 |

## 整合对话已发现的前端契约对齐问题（F2 待修）
1. `createRecord` URL 应为 `/record`（前端误写 `/record/create`）
2. `getDailyStat` 入参字段应为 `{ date?: string }`（前端误用 `{ recordDate: date }`）
3. `getRecordList` 响应应为 `PageData<RecordItem>`（前端误用裸数组 `RecordItem[]`）
4. 缺 `api/upload.ts`（需新增 uploadImage 接口封装 + mock）

## 阻塞项
- 用户需提供：小程序 AppID、GLM-4V API Key、PG 连接信息（填 packages/server/.env，不入仓）

## 下一步动作
1. 前端拉 main → 修 4 项对齐 → 重新 push feature/fe-scaffold
2. 服务端拉 main → 派发 user/food/record/upload 模块实现 + AI 运行时接入
3. 三端联调（env.useMock 切换真实接口）

## 历史决策变更
- 2026-06-29: DB 选型确定为 PostgreSQL
- 2026-06-29: AI 供应商确定为 GLM-4V（视觉）+ MiniMax M3（文本，后续）
- 2026-06-29: recognize 接口不自动落库（避免孤儿记录，由 API 对话设计决策）
