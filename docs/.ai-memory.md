# semo 进行中任务（整合对话维护）

> 记录当前正在推进的任务、阻塞项、下一步动作。整合对话每次会话结束更新此文件。

## 当前阶段：MVP 第一波已完成，待真机联调
- 契约 + 前端 + 服务端三端 MVP 第一波全部合并入 main（fc02f8a）
- 联调校验通过：4 项契约对齐确认，recognize 调用链跑通
- 技术决策已定：DB=PostgreSQL，AI视觉=GLM-4V，文本(计划生成)=MiniMax M3(后续)

## 已完成里程碑
- 961c7e1 整合对话初始化骨架（shared/ai/mp/server+docs）
- 614954d API 对话补全 MVP 契约（user/food/record/upload）
- 7510191 整合对话评审通过，契约合并入 main
- 0480165 前端补同步契约（4 项对齐：URL/字段/分页/upload接口）
- 206e94f 服务端 MVP 第一波（user/upload/food/record + GLM-4V 运行时）
- 8ab221d 前端脚手架合并入 main
- fc02f8a 服务端合并入 main（MVP 第一波集成完成）

## 联调校验结果（fc02f8a）
| 项 | 服务端实现 | 前端调用 | 状态 |
|---|---|---|---|
| record 路由 | POST /record, GET /record/list, GET /record/daily-stat | api/record.ts 一致 | ✓ |
| daily-stat 入参 | dto.date | { date } | ✓ |
| list 响应 | PageData<RecordItem> | 期望 PageData<RecordItem> | ✓ |
| recognize 调用链 | resolveImageForGlm 识别 /static/ 前缀→读盘转 base64 | 传相对 URL /static/uploads/... | ✓ |

## 跨端协同补丁项（下一波）
- **F3**: record list 页展示历史打卡图片时，RecordItem.imageUrl 是相对 URL，mp-weixin 无法直接显示，需拼 server origin
  - 影响范围：pages/record/index.vue 历史列表展示、pages/stats/index.vue（如有图片）
  - 不阻塞主流程（拍照→识别→打卡→统计已跑通）
  - 修复方案：前端加 utils/resolve-url.ts，相对 URL 拼 env.apiBaseUrl

## 阻塞项（待用户操作）
- **U1**: 填 packages/server/.env（真实值不入仓）：
  - GLM_API_KEY（智谱，格式 id.key）
  - DB_HOST/DB_PORT/DB_USERNAME/DB_PASSWORD/DB_DATABASE（PG）
  - JWT_SECRET
  - MP_APPID/MP_SECRET（微信，不填则 login 走 mock openid）
- **U2**: 真机联调验证（微信开发者工具 + server 启动）

## 进行中任务
| ID | 任务 | 归属 | 状态 |
|---|---|---|---|
| U1 | 填 server .env | 用户 | 待办 |
| U2 | 真机联调 | 用户 | 待办 |
| F3 | record list 图片 URL 拼接 | 前端对话 | 待派发（下一波） |

## 下一步动作
1. 用户填 .env + 启动 server（pnpm --filter @semo/server start:dev）
2. 用户用微信开发者工具打开 packages/mp，配 AppID，env.useMock=false
3. 真机走一遍：登录 → 拍照 → 识别 → 打卡 → 统计
4. 联调通过后，派发 F3（图片 URL 拼接补丁）+ 第二波（计划生成 MiniMax M3）

## 新增技术债（服务端自报）
- TD007: TypeORM synchronize 仅 dev 开启，生产需关闭改用 migration
- TD008: tsconfig 配 ignoreDeprecations + strictPropertyInitialization:false（NestJS+TypeORM 妥协）
- TD009: user login 微信 jscode2session 走 mock openid（待 MP_APPID/SECRET 填后接真实）

## 历史决策变更
- 2026-06-29: DB 选型确定为 PostgreSQL
- 2026-06-29: AI 供应商确定为 GLM-4V（视觉）+ MiniMax M3（文本，后续）
- 2026-06-29: recognize 接口不自动落库（避免孤儿记录）
- 2026-06-30: MVP 第一波三端集成完成，联调校验通过
