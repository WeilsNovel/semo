# semo 进行中任务（整合对话维护）

> 记录当前正在推进的任务、阻塞项、下一步动作。整合对话每次会话结束更新此文件。

## 当前阶段：项目初始化收尾
- 状态：骨架已落盘，待用户环境验证
- 阻塞项：
  - 用户尚未执行 `pnpm install`（需本地有 Node≥20 + pnpm≥9）
  - 用户尚未提供：小程序 AppID、AI 供应商与 Key、DB 选型
- 下一步动作：
  1. 引导用户执行 `pnpm install` 与 `pnpm -r typecheck` 验证骨架
  2. 收集用户提供的 3 项环境信息（敏感值不入仓）
  3. 产出第一个迭代需求拆解（建议 MVP：微信登录 + 拍照识别 + 落库打卡）

## 任务清单（按优先级）
| ID | 任务 | 归属对话 | 状态 |
|---|---|---|---|
| T1 | 用户环境验证 pnpm install | 用户 | 待办 |
| T2 | 提供小程序 AppID / AI Key / DB 配置 | 用户 | 待办 |
| T3 | 第一个迭代需求拆解（MVP） | 整合对话 | 待办 |
| T4 | shared 补全 user 模块完整 DTO | API 对话 | 待办 |
| T5 | server user 模块实现（登录+档案） | 服务端对话 | 待办 |
| T6 | mp 登录页 + 拍照页骨架 | 前端对话 | 待办 |

## 跨端协同待办
- 无（首次需求尚未拆解）

## 历史决策变更
- 无
