{
  "name": "@semo/shared",
  "version": "0.1.0",
  "private": true,
  "description": "semo 多端共享契约层（DTO/错误码/枚举）单一事实来源",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
